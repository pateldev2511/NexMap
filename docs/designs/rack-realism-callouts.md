# CEO Plan: Rack Realism, Rich Callouts & Export Fidelity
Generated by /plan-ceo-review on 2026-07-09
Branch: main | Mode: SELECTIVE EXPANSION
Repo: pateldev2511/NexMap

## Problem (user brief, 2026-07-09)

1. Rack gear isn't realistic — e.g. a 24-port patch panel renders 2×12; real ones are 1×24.
2. Device names/model names should move OFF the faceplate into movable rich-text
   callouts (heading/subheading/description; bold, italic, ordered/unordered lists,
   code box, left/right/center/justify) connected to the unit by an editable colored
   dotted leader line — and they must appear in exports.
3. BUG: Network Designer exports don't show the link arrows visible on canvas.
4. BUG: The "More" button (top-right) doesn't work.
5. PERF: ISO mode feels laggy after recent updates; make it smooth without losing features.
6. Add tooltips so users know what everything does.
7. Update README.md with all new features. Act as a professional tester over everything.

## Audit evidence driving this plan (verified 2026-07-09 at 65bb211)

- **More menu**: REGRESSION from the v0.6.2 CI fix. `.topbarActions` became a scroll
  container (`overflow-x:auto`, AppShell.module.css:98); the dropdown `.menuPanel`
  (position:absolute inside it, :187) is clipped. Menu opens invisibly. Third
  `.topbarActions` incident in a week — fix must move the panel out of the clip
  context (portal), and this surface gets its own e2e.
- **Export arrows**: canvas renders `<marker id="nexmap-arrow">` + markerStart/End
  from `link.arrow` (Canvas.tsx:1506,1607); `src/io/export/buildSvg.ts` contains zero
  marker code. Exports have never had arrows. Information loss, not just cosmetics.
- **Patch panel**: `portLayout()` (rackLayout.ts:133) uses a blanket `rows = n<=8 ? 1:2`.
  24-port panel → 2×12. Real 1U panels are 1×24 grouped in 6s. Real 24/48-port
  SWITCHES genuinely are 2 staggered rows (odd top/even bottom — current column-major
  fill already produces this) → fix is PER-KIND profiles, not "always one row."
- **ISO lag**: (a) `IsoGrid`'s `flat` prop is a fresh object built inline per render
  (Canvas.tsx:1535) → re-renders every pan frame; (b) the link layer rebuilds per
  frame (deferred task #9), and iso pays extra per link (shadow path + counter-rotated
  labels). Iso device nodes are already memo'd correctly (v0.6.2).
- **Leverage**: plain-text note objects + IsoTextNode exist (Phase 9.4); `title=`
  attributes partially cover chrome; export builders are string-based with tests
  (buildSvg.test, buildRackSvg.test); toolbarPlace/History/commands all reusable.

## Chosen approach (user-approved)

**Callout architecture: Approach B — structured rich text, dual-rendered.**
Because exports must show callouts, the text model is a constrained block structure
rendered to native SVG text — never HTML/foreignObject (breaks PNG converters and
design-tool imports). Zero new runtime dependencies.

### Callout data model (schema v4 addition)

```ts
type RichSpan   = { text: string; bold?: true; italic?: true };
type Align      = 'left' | 'center' | 'right' | 'justify';
type CalloutBlock =
  | { kind: 'heading' | 'subheading' | 'paragraph'; spans: RichSpan[]; align: Align }
  | { kind: 'bullets' | 'numbers'; items: RichSpan[][] }
  | { kind: 'code'; text: string };            // mono, boxed; no inline marks

type CalloutAnchor =
  | { type: 'device'; id: string }             // flat device / rack device / object
  | { type: 'point'; x: number; y: number }    // detached (survives target deletion)
  | null;                                      // free note, no leader

interface Callout {
  id: string;
  scope: 'flat' | `rack:${string}`;            // which canvas owns it (rackId-scoped)
  x: number; y: number;                        // box top-left, scene coords
  width: number;                               // wrap width (resizable)
  blocks: CalloutBlock[];
  anchor: CalloutAnchor;
  leader: { color: string; dash: 'dotted' | 'dashed' | 'solid'; width: number };
  role?: 'callout' | 'title-block' | 'legend'; // cherry-pick 3 reuses the renderer
}
```

- Stored in projectStore as a first-class collection; Add/Update/Delete commands with
  `mergeWith` for drag coalescing and `isIdentity()` (v0.6.2 History contract).
  One gesture = one undo entry, including create-with-content.
- `.nexmap` schema bumps to v4. FORWARD-COMPAT DECISION (spec-review corrected: the
  loader deliberately REFUSES newer-than-supported schemas — migrate.ts:107, pinned
  by migrate.test.ts:63 as a data-safety property): older app builds will refuse v4
  files with the existing clear "too-new" error rather than silently dropping
  callouts. That is the correct existing behavior; the plan keeps it. v3 files load
  into v4 with `callouts: []` (round-trip tested).
- Zoom semantics: callouts are SCENE-SPACE objects — text and leader scale with
  viewport zoom exactly like devices (this is what makes export parity trivial).
- Selection semantics (flat): callouts participate in click-select, marquee/lasso,
  multi-select drag, duplicate, and copy/paste like other canvas objects. Rack focus
  view: single-select + drag only in v1.
- Rack deletion deletes its scoped callouts INSIDE the same delete transaction
  (one undo restores rack + callouts together).

### One renderer, two hosts (parity by construction)

`calloutParts(callout, opts): string[]` — pure SVG-string builder in the
rackDeviceArt style, used by BOTH the live canvases (via `dangerouslySetInnerHTML`,
same pattern as rack art; all user text through `escapeXml`) AND buildSvg/buildRackSvg.
There is no second renderer to drift. Text wrapping via a cached canvas-2D
`measureText` with the app font stack; justify approximated with per-line
`word-spacing` — a documented limitation (some SVG→PNG converters honor it
inconsistently). Verification is priced concretely: the W6 parity e2e injects the
exported SVG into the live page and compares justified line widths via getBBox()
against the wrap width (no new raster infra — the Playwright page IS the renderer);
external-converter variance stays a documented limitation. Iso: box renders upright via the existing
ISO_COUNTER matrix (IsoTextNode pattern); leaders render in the screen-space overlay
layer (see Leader line section) — the box is screen-aligned, so leader geometry is
computed post-projection. ARCHITECTURAL REQUIREMENT (spec-review iter 2): iso
exports flow through the separate `buildSvgIso` path, so leader endpoint math MUST
live in one shared pure function — `leaderGeometry(box, targetBbox, projection) →
{x1,y1,x2,y2}` — consumed by the FLAT renderer, the live iso overlay, AND
buildSvgIso (all three; parity-by-construction for every leader everywhere).
Implementation note: W6 endpoint assertions use numeric tolerance (±0.5), never
exact float string matching. Without this
extraction the parity-by-construction claim is false for iso; it is a W3
deliverable, not an option.

### Editor (in-canvas, no deps)

RISK NOTE (spec-review): the editor is the highest-risk piece of this plan —
cross-block caret management, Safari span-splitting, and IME composition make
freeform multi-block contentEditable 2-3× costlier than it looks. V1 therefore uses
SINGLE-BLOCK EDITING: exactly one block is editable at a time (click a block to edit
it; one contentEditable). Enter commits the block and creates/focuses the next;
Backspace at the start of an EMPTY block deletes it and focuses the previous block's
end; there is NO cross-block text selection or caret traversal in v1. IME: commit
only on compositionend. Paste: plain text only (length-capped at 10k chars — the
hostile-QA 5MB paste must not freeze the UI), marks re-applied manually.
Parser: restricted DOM→`RichSpan[]` (only `<b>/<strong>/<i>/<em>`+text survive;
everything else stripped → XSS-safe by construction, no raw HTML ever stored).
Toolbar: block-type select (Heading/Subheading/Paragraph/Bullets/Numbers/Code),
B, I, align ×4, leader color swatches + dash select, delete. Keyboard: Cmd+B/I,
Shift+Enter=soft break, Escape=commit+close (router overlay while open — v0.6.2
QuickCreateMenu lesson: keys must not leak to canvas). Double-click a callout opens
the editor; the callout body is a normal machine-owned drag target (4px threshold).

### Leader line

From nearest box edge to the target's bbox edge; recomputed when target moves.
Editable color (CABLE_COLORS palette + custom), dash preset, width (scene units).
Default: dotted, accent color. Target deleted → the STORED anchor is untouched;
resolution is LAZY at render time: unresolvable target ⇒ leader simply not drawn
(box stays visible and selectable). Undoing the device deletion restores the leader
automatically with zero extra history entries — the one-gesture-one-undo contract
holds by construction. Explicit permanent detach (anchor → point/none) is available
in the editor. In ISO mode, leaders render in the SCREEN-SPACE overlay layer
(outside the iso matrix): endpoints = the upright box edge and the projected target
bbox edge, both computed post-projection in screen coordinates.
Width resize: right-edge handle, min 120 / max 480 scene px, machine-owned drag
coalescing to one undo entry (same contract as move).
Selecting a callout shows leader controls in the selection toolbar (M3 pattern).

### Label policy (user decision, cherry-pick 2)

Faceplate labels STAY by default (realism). Callouts are opt-in per device
("Add callout" in device toolbar/context menu, pre-filled: heading=name,
subheading=vendor+model). **Annotate all** command (rack designer): generates
callouts for every mounted device on the visible face, skipping devices that already
have one; idempotent on re-run. Column layout: x = rack right edge + 40 scene px,
box width 220, stacked top→down in U order with 12px gaps; if user-placed callouts
already occupy the column, generated boxes continue below the lowest occupant
(no overlaps within the column). Straight leaders to each device's right bbox edge;
leaders may cross on dense racks in v1 — accepted, documented. One transaction, one
undo. Per-rack `hideFaceplateText` toggle suppresses `labelParts` on faceplates
(canvas + export; row view unaffected). Export bounds MUST union callout boxes
(W4 acceptance criterion — a margin column clipped out of the export would defeat
the feature).

## Accepted Scope

### W1 — Fixes first (each its own commit)
- **More menu portal**: `createPortal` the panel to body; position from summary
  rect; close on outside-pointerdown, Escape (router overlay), resize/scroll;
  `aria-expanded`. E2e: opens + item clickable at default AND 1100px widths.
- **Export arrows**: the canvas uses ONE neutral marker (`fill: var(--chrome-fg-muted)`,
  Canvas.tsx:1514) — not per-color (spec-review verified). buildSvg emits one marker
  def with the CSS var RESOLVED to the export theme's literal hex (CSS vars are
  forbidden in exports — non-browser consumers), plus markerStart/End per link's
  `arrow` field. Unit test in buildSvg.test + parity coverage (W6).
- **ISO perf**: IsoGrid bounds quantized to grid multiples with over-draw margin,
  memo'd on primitive bounds (re-renders only when crossing a cell boundary);
  extract memoized `LinkLayer` (keyed rev/selection/projection/health/linkTarget)
  with the scale-dependent relink handles in a tiny live overlay — absorbs the
  perf half of task #9. Perf pins: isoScene.perf.test.tsx + link-layer counter in
  flatScene.perf.test.tsx (30 wheel-pans → 0 scene re-renders, pan-really-happened
  guard — v0.6.2 pattern).

### W2 — Rack realism (per-kind faceplate truth)
- `portLayout` gains kind profiles as a DATA TABLE (`KIND_LAYOUT:
  Record<PanelKind, LayoutProfile>`), not an if/else chain. Taxonomy reality
  (spec-review corrected): `panelKind.ts` collapses router/load-balancer/
  access-point/wireless-controller into `'switch'`, and `ups` → `'psu'`; there is
  no `pdu` kind today. W2 therefore has two tiers:
  (a) IN-KIND fixes, no type changes: `patch` → single row, groups of 6 with group
      gap (24 → 6|6|6|6); `switch` → keep 2-row odd/even stagger, add a bank
      separator every 6 columns; `server` profile audit.
  (b) TAXONOMY EXTENSION (only if (a) proves insufficient for realism): new
      PanelKind values (e.g. `pdu`) — a type-signature ripple through
      rackDeviceArt/buildRackSvg/portHit, priced separately and decided at
      implementation time with the eng review.
- Port NUMBERS stay legible at export scale (existing `patchText` labels).
- Update rackLayout tests + buildRackSvg snapshot-ish assertions; port hit-testing
  (`devicePortLayout` consumers: cabling drag/click-click) must keep working — the
  e2e cabling suite is the regression net.

### W3 — Callout core (flat canvas first)
Model + commands + store + editor + leader + machine-owned move/select + iso
upright + `.nexmap` v4 persistence + export via calloutParts + validation-safe
(callouts excluded from network validation). Flat export bounds (contentBounds)
union callout boxes — a callout dragged past the diagram edge must not clip out of
the export (same criterion as W4's rack margin column). E2e: create→format→move→export;
undo = one entry; Escape/key-leak; target-move re-anchors leader.

### W4 — Rack integration + annotate-all
Callouts on rack focus canvas (scoped per rack, exported in elevation), device
toolbar "Add callout", Annotate-all command + margin column layout,
`hideFaceplateText` toggle (canvas + export + row view unaffected). E2e: annotate-all
on a template rack; toggle hides faceplate text in export string.

### W5 — Title block & legend (cherry-pick 3)
`role:'title-block'` (project name/author/date/revision — generated, editable) and
`role:'legend'` (link colors/styles + rack cable colors with labels). Legend content
is SNAPSHOT-AT-GENERATION, thereafter an ordinary editable callout; a "Regenerate"
action replaces its content (documented in-UI). Positioned inside export bounds;
both are plain callouts underneath (no new renderer).

### W6 — Export parity harness (cherry-pick 1)
E2e fixture project (links with arrows ×3 styles, dashed links, labels, callout with
every block type, rack with patch panel + hidden faceplates + title block, AND an
ISO export of the annotated diagram — leader geometry parity); export SVG strings
captured in-browser; assertions that canvas-visible features exist in the export
(marker refs, dash arrays, text content, callout boxes, iso leader endpoints), plus
the in-page getBBox justify check. Runs in CI with the
suite — this bug class dies here.

### W7 — Tooltips, README, tester pass
- Tooltips (mechanism DECIDED in review: custom layer): a dependency-free CSS
  tooltip — chrome controls get `data-tip="What it does (⌘K)"` rendered via a
  single global `[data-tip]::after` rule (≈300ms transition-delay, styled to the
  chrome theme, `@media (hover: hover)` so touch never shows stuck tips), with
  `aria-label` kept in sync for screen readers; `title=` removed where `data-tip`
  lands (no double tooltips). Canvas SVG affordances (ports, connect handles,
  cables, chevrons) get SVG `<title>` children. Coverage: every button/select/
  input in AppShell, both designers' toolbars, zoom clusters, selection toolbars,
  panels, dialogs — audited against a checklist in the tooltip commit, with a
  smoke e2e asserting presence on key controls.
- README: rewrite features section to current reality (pointer-native input, touch,
  quick-create, quiet canvas, callouts, realism, exports/parity, tooltips) +
  screenshots refresh note.
- Tester pass: full gates (tsc/unit/e2e) + `/qa` browser sweep of every feature
  area listed in README; findings fixed or filed before ship.

## NOT in scope
- Freeform HTML / RTE dependency (violates export fidelity + no-deps ethos).
- Full auto-migration of every label to callouts as the default (spaghetti; annotate-all
  command covers the intent on demand).
- Leader-line collision routing around gear (v1 allows crossings; revisit with demand).
- Callouts in the multi-rack ROW view (focus view + exports only in v1; row view is
  an overview).
- Rich text in device names/link labels (callouts only).
- The dedup half of task #9 (adapter/useGestureMachine refactor) — unchanged, still
  tracked in task #9's remainder.

## Error & edge-case contract (highlights; full registry in review)
- ContentEditable parser: unknown tags stripped, never stored; paste = plain text +
  marks re-applied manually; empty callout on commit → deleted (no ghost boxes).
- Anchor target deleted → stored anchor untouched; leader lazily not drawn while
  unresolvable; undo of the deletion restores it with zero extra history entries.
- measureText cache invalidated on font-load (document.fonts.ready → re-render once);
  the EXPORT path awaits document.fonts.ready before measuring, so an export
  triggered pre-font-load cannot bake wrong wrap positions into the file.
- Export with zero callouts → byte-identical shape to today (no regressions for old
  projects); schema v4 load/save round-trip tested; v3 file load tested.
- Annotate-all on empty rack → no-op with announce("Nothing to annotate").
- IsoGrid quantization at extreme zoom (0.1): bounds capped to finite grid count
  (MAX_GRID_LINES guard) — a 10× over-zoom must not mint 10k lines.
- More-menu portal: outside-click closes before canvas sees the pointerdown
  (capture-phase), Escape consumed by overlay, menu closes on ANY action click.

## Test plan (professional-tester ask, made concrete)
- New unit: calloutParts (every block type, wrapping, escaping, justify), parser
  round-trip (DOM→spans→DOM), portLayout per-kind profiles, buildSvg markers.
- New perf pins: isoScene, link-layer.
- New e2e: more-menu (2), export-arrows (1), callouts flat (4), rack callouts +
  annotate-all + hide-faceplate (3), title-block/legend (2), export-parity (1 big),
  tooltips smoke (1). Existing e2e suite (16 spec files) must stay green (cabling suite = port-layout
  regression net).
- Post-implementation: /qa sweep, README-vs-behavior audit.

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | Export parity harness | M | ACCEPTED | Kills the export-drift bug class permanently |
| 2 | Selective callouts + Annotate-all + hide-faceplate toggle | M | ACCEPTED | Honors the ask without spaghetti; matches real elevation docs |
| 3 | Title block & legend | M | ACCEPTED | Same renderer, document-grade exports |

## Deferred to TODOS.md
- (none new — task #9 remainder [adapter dedup, color swatch] unchanged)

## Constraints
- No new runtime dependencies. Local-only (CSP + netguard untouched).
- Every gesture = exactly one undo entry (History identity contract from v0.6.2).
- Exports must render in non-browser consumers (no foreignObject anywhere).
- Existing e2e suite (16 spec files) + the current unit suite (~690) stay green at every milestone commit.
