---
status: ACTIVE
---
# CEO Plan: Pointer-Native Canvas (both designers)
Generated by /plan-ceo-review on 2026-07-03
Branch: main | Mode: SELECTIVE EXPANSION
Repo: pateldev2511/NexMap
Pinned to: commit 3568562 (+ uncommitted RackCanvas.tsx/RackDesigner.module.css
pan/zoom work in the tree — see Milestone M2 note). Line refs must be
re-verified at implementation time. CAVEAT (eng-review outside voice, finding
1): parts of the original audit ran >=7 commits behind HEAD — treat every
line ref and "this doesn't exist yet" claim as a hint to verify against HEAD,
never as fact. Two review-added work items were caught scheduling
already-shipped code (cablePath helper, connectRackCable validation, both in
2ad076d) and have been removed.

## Problem (user brief, 2026-07-03)

Both designer canvases (flat network designer + rack designer) must be mouse
AND touchpad friendly. All interaction edge cases handled, with logical proof
(tests), never "trust me it works." Less distracting chrome; everything doable
in-canvas ("have everything can be done inside the canvas too, like drag and
drop options for connections, and other experiences like that"). FossFLOW-
inspired feel, all code from scratch, everything runs locally.

## Audit evidence driving this plan

- Two pointer-capture regressions already shipped (Phase 8 latent capture bug
  that broke 5 gestures; v0.6.1 rack pan stealing clicks). Zero tests dispatch
  WheelEvent/PointerEvent/TouchEvent across 61 unit files + 2 e2e specs.
- The two designers have OPPOSITE wheel semantics: flat = wheel-pan +
  ctrl/pinch-zoom (Canvas.tsx:402-418); rack = wheel-always-zoom, no pinch
  distinction (committed: RackRow.tsx:243-250, factor 1.1; the uncommitted
  RackCanvas.tsx worktree diff adds a second wheel-always-zoom handler). No
  deltaMode normalization anywhere.
- THREE gesture implementations, not two: Canvas.tsx (flat), RackCanvas.tsx
  (rack focus editor), and RackRow.tsx (~260-272: its own pan + pointer-capture
  code, 4 client-px threshold). All three become consumers of the shared core.
- 11 provable edge-case gaps (see Error Registry in the review): no
  pointercancel handling; Cmd+Z fires model-undo inside text inputs
  (App.tsx:306-327); undo-during-drag corrupts positions via stale dragOrigins;
  silent rejected rack drops (RackDesigner.tsx:368 discards FitResult); Escape
  cannot cancel rack cable drag; stale flat<->iso projection closures
  (Canvas.tsx:465-497); rack thresholds in SVG units (20x feel variance across
  zoom); ghost marquee (no e.buttons check); justMarqueed click swallow; stale
  rack fit on rack switch; dead library filter pills (RackDesigner.tsx:699-704).

## Chosen approach (user-approved)

APPROACH B — Shared interaction core in `src/input/`.

Considered and rejected (outside-voice finding 9, user re-confirmed B): a
minimal version (11 point fixes in place + extract only normalizeWheel + a
small shared wheel hook + e2e specs, ~1/3 the cost). Rejected because: (a)
Approach A was priced at selection time and the user chose B knowingly; (b)
touch (M4a) and cross-rack pointer drag (M4d) are ACCEPTED scope that
requires the shared machine; (c) with the harness hardened (fixture
provenance, capture-regression e2e, Safari exception) the provability
argument B rests on is honest, not theater.

### Wheel contract (all three canvases)

| Preference mode | Plain wheel | Ctrl/Cmd+wheel or pinch | Shift+wheel | Pan fallbacks (always) |
|---|---|---|---|---|
| `pan` (DEFAULT) | pan (both axes) | zoom at cursor | horizontal pan | Space+drag, middle-drag, right-drag, Hand tool |
| `zoom` | zoom at cursor | zoom at cursor | horizontal pan | Space+drag, middle-drag, right-drag, Hand tool |

`wheelAction: 'pan' | 'zoom'` (eng-review outside voice findings 2+3 —
SUPERSEDES the earlier tri-state `auto` heuristic): default `pan` matches
today's flat-canvas behavior and DA-DES-5.1 for EVERY device, so nothing
changes under anyone's fingers by default on either input class. `zoom` is
the explicit opt-in that restores wheel-zoom (including for former rack-view
habits). The device classifier is DEMOTED to delta normalization only — its
mode-picking spec was provably wrong on Firefox deltaMode=1 hardware
(discrete wheels report |deltaY| ~ 3 lines and would misclassify as
trackpad), and heuristic mode-picking contradicted DA-DES-5.1's
unconditional scroll=pan on the flat canvas. Figma's real default is the
same shape: scroll pans; wheel-zoom is a preference.
Momentum-tail swallow: after Escape-cancel or modal-open, inertial wheel
events are ignored until sign-change or 150 ms of quiet.

Space+drag pan (DA-DES-5.1's explicit fallback, already implemented on the
flat canvas via `spaceHeld`) is a first-class state in the shared machine and
extends to BOTH rack canvases; middle-drag pan is kept everywhere. This is the
documented vertical-pan path for `mouse` mode, where plain wheel zooms.

- Preference lives in `src/lib/prefs.ts` (same union-guard pattern as
  `getConnectMode()`; garbage stored values fall back to `pan`), surfaced in
  SettingsDialog next to the connect-mode setting.
- Migration note: rack users lose wheel-zoom-by-default (intentional; it
  violated DA-DES-5.1). One-line changelog entry + the Settings toggle
  restores it via `zoom` mode. Flat-canvas default behavior is UNCHANGED for
  all devices.
- Normalization: `normalizeWheel(e)` converts deltaMode LINE(1)->x16 px,
  PAGE(2)->x100 px, then clamps |delta| to <=120 px per event BEFORE applying
  zoom factor `Math.pow(1.0015, -delta)`. Pinch events (ctrlKey, typical
  |deltaY| 0.5-3) pass through unclamped below the cap; discrete wheel notches
  (|deltaY| ~100-150) hit the cap, giving ~1.2x per notch. The 120 px clamp
  applies to the ZOOM branch only; the pan branch uses normalized but
  unclamped deltas (fast free-spin wheel scrolling stays fast). Unit-tested
  with recorded real-device sequences: captured via a dev-only normalized-
  event logger behind a `?debug=input` URL flag, checked in as JSON fixtures
  under `src/input/__fixtures__/`.
- Unified scale bounds: MIN 0.1, MAX 4.0 (flat's current bounds; rack's floor
  widens from 0.2 -> 0.1 — visible but harmless change). One `zoomAt(vp,
  factor, screenX, screenY)` signature; rack/viewport.ts's incompatible
  `(vp, px, py, factor)` is retired. API break is internal only.

### Gesture state machine

- Pure-function core: `reduce(state, normalizedEvent) -> {state, effects[]}`.
  No DOM access inside the machine — this is what makes it provable (see Proof
  harness). Thin DOM adapters per canvas own capture/listeners only.
- SHAPE (outside-voice finding 6, decided): a shared LIFECYCLE SKELETON
  (idle -> armed -> threshold -> active -> commit/cancel, plus pan/pinch/wheel
  states) owns the capture/cancellation/e.buttons/threshold invariants; each
  canvas contributes a typed gesture TABLE (flat: drag/marquee/lasso/link/
  relink/resize/waypoint/shape; rack: device-move/cable/marquee; row:
  pan/drill). NOT one union machine spanning ~15 gesture kinds. The fuzz
  invariant runs against the skeleton, so every canvas inherits the
  no-stuck-gesture guarantee.
- KEYBOARD ROUTER (outside-voice finding 5, accepted): one window keydown
  owner in `src/input/`; the active canvas registers its machine; routing
  order = gesture-cancel -> canvas shortcuts -> app shortcuts. The three
  existing competing listeners (App.tsx:337, Canvas.tsx:378, RackDesigner.tsx
  :254) consolidate onto it in M1. This is where "Escape cancels the
  innermost thing" and "undo aborts the drag first" become enforceable.
- First-class cancellation: `pointercancel` and `lostpointercapture` are
  transitions to `none` with cleanup effects; Escape cancels the innermost
  active thing only (gesture -> armed state -> selection, one per press).
- `e.buttons` validated on every move (kills ghost marquee/pan residue).
- Second-pointer policy, decided per state AND pointer type (outside-voice
  finding 8, accepted): pointerType 'touch' second finger during an active
  gesture -> CANCEL the gesture (revert the item) and enter PINCH — the
  universal touch escape hatch (tldraw/FigJam pattern), decided now so M4a
  never reopens the transition table. On idle touch canvas -> two pointers
  enter PINCH directly. pointerType 'mouse'/'pen' second button mid-gesture
  -> ignored (captured gesture keeps ownership).
- Thresholds in CSS px (converted from SVG user units wherever a canvas
  applies a scale transform) before the machine sees them: 4 px click-vs-drag
  (flat parity), same value for rack marquee/cable arming (replaces the
  6 SVG-unit threshold whose feel varied 20x across the zoom range).
- Wheel handler scoped to the canvas surface, not the root container: events
  whose target sits inside floating chrome (toolbar, minimap, zoom bar,
  panels) are not consumed, fixing the current flat-canvas bug where
  scrolling over the toolbar pans the canvas (Canvas.tsx:402-418 listens on
  the root div).
- Safari trackpad pinch: handle `gesturestart`/`gesturechange`/`gestureend`
  (preventDefault + map scale delta to zoomAt) in the adapter, since Safari
  emits proprietary gesture events for pinch; ctrlKey-wheel remains the
  Chromium/Firefox path.
- Right-drag pan extends to both rack canvases for parity with flat
  (contextmenu suppressed after a right-drag, same `suppressMenu` pattern).

### Adapter & router contract (eng review 2026-07-03)

- EFFECTS EXECUTION (two classes, eng-review outside voice finding 4):
  SYNC-MANDATORY effects (`preventDefault`, `capture`, `release`,
  commit-`dispatch`) execute synchronously inside the originating DOM event
  handler — preventDefault is dead if deferred. VIEWPORT WRITES are applied
  synchronously to a viewport REF (order-preserving: pan deltas and
  cursor-anchored zoomAt apply in event order, so there is no commutativity
  problem), and the ref is FLUSHED to React state at most once per animation
  frame. A pending flush is forced on gesture end/cancel and cancelled on
  unmount. The reducer is never invoked during render.
- STATE RESIDENCE: machine state lives in a ref inside each canvas adapter
  (same lifetime as today's `gesture.current`). No React state for gesture
  progress; only committed results flow through the store.
- ROUTER REGISTRY API: `registerCanvas(id, {cancelActiveGesture(),
  hasActiveGesture(), handleKey(e), handleKeyUp(e)})` — handleKey carries the
  canvas-shortcut stage (arrow-nudge, Delete, mode keys); handleKeyUp carries
  Space/Alt release. Registration is StrictMode-IDEMPOTENT (Map set/delete;
  mount->unmount->mount is symmetric — main.tsx runs StrictMode, and this is
  tested). Only one canvas is mounted at a time (App mode is 'rack'|'network'
  exclusive), so "active" = "registered", no focus model needed.
- ROUTING ORDER (full): OVERLAYS first (context menu, command palette,
  dialogs, presentation-mode Escape — the six existing window keydown
  listeners: App.tsx x3, Canvas.tsx, ContextMenu.tsx:38, RackDesigner.tsx are
  each either absorbed or explicitly registered in this layer) -> gesture-
  cancel -> canvas shortcuts (handleKey) -> app shortcuts. "Escape cancels
  the innermost thing" now includes overlays as the innermost layer.
- UNDO MID-GESTURE (decided — supersedes behavior change 7's earlier
  wording): Cmd+Z during an active gesture is CONSUMED by the cancel — the
  gesture reverts, history is untouched; a second press performs the actual
  undo (Figma/tldraw behavior; bailing out of a bad drag must not eat your
  last committed edit).
- ROUTER FAULT ISOLATION with ABORTED CONTINUATIONS: handlers are wrapped in
  try/catch with a dev warn, and a throw ABORTS the dependent continuation —
  if `cancelActiveGesture()` throws, `store.undo()` is NOT called (undo over
  a live drag is the stale-dragOrigins corruption this plan exists to kill).
  A throw in one layer never blocks unrelated layers (Cmd+S still saves).
- UNREGISTER-DURING-ACTIVE-GESTURE: unregistering (mode switch mid-drag,
  unmount) first cancels the active gesture, releases capture, and reverts
  the in-flight item. Tested.
- LEGACY SHIM (strangler seam): from M1 commit one, `registerCanvas` accepts
  an implementation backed by the LEGACY `gesture.current` refs. Unmigrated
  gestures stay Escape-cancellable and undo-safe through the router for the
  whole migration window; each per-gesture migration commit swaps that
  gesture's shim path for the machine path. Rack keydown consolidation
  happens in M2 (rack machines don't exist in M1 — the shim covers rack for
  the gap only if M2 starts before M1's router removes RackDesigner's
  listener; simplest rule: RackDesigner's listener is absorbed in M2, not
  M1).
- RACK GUARD NON-REGRESSION: RackDesigner's existing keydown already guards
  INPUT/SELECT/TEXTAREA — consolidation must preserve that (the shared
  isTextTarget gains SELECT and is used by the router globally).
- M1 ADOPTION STRATEGY (decided): strangler-fig, per-gesture. Land the
  skeleton + wheel/pan/pinch with behavior identical, then migrate flat
  gesture kinds one commit each: drag -> marquee/lasso -> link/relink ->
  resize/waypoint/shape. Old and new coexist briefly inside Canvas.tsx; every
  commit lands green and bisectable. No big-bang swap.
- NO ONE-SHOT FLAGS (code quality rule): trailing-click swallowing and its
  relatives are modeled as machine STATES (e.g. `swallowNextClick` expires on
  the very next pointerdown/up pair), never as mutable boolean refs. The
  `justMarqueed` leak class becomes unrepresentable.
- RAF BATCHING: pointermove-driven viewport writes coalesce to one setState
  per animation frame in the adapters (matters most for RackRow, where every
  pan frame currently re-renders the whole scene).
- FUZZ DETERMINISM: the fuzz spec uses a seeded PRNG with the seed printed on
  failure, and a CI runtime budget of ~2 s (thousands of sequences, pure
  function — cheap).

### Proof harness (the jsdom problem, solved by construction)

jsdom does not implement pointer capture or real layout, so the plan does NOT
dispatch PointerEvents at jsdom DOM. Three layers instead:

1. UNIT (vitest, no DOM): the pure state machine is fed recorded normalized
   event sequences (mouse drag, trackpad pan, pinch, touch two-finger, cancel
   mid-drag, Escape, second-pointer attack) and asserted transition-by-
   transition. This is where every edge case in the registry gets its proof.
   Plus a FUZZ spec: thousands of random event sequences asserted against
   two invariants — the reducer never throws, and from any state a
   pointercancel/Escape pair always returns to `none` with no residual
   armed/capture effects (the "no stuck gesture, ever" guarantee).
2. ADAPTER UNIT (vitest + jsdom): `normalizeWheel`, threshold conversion, and
   the capture-wrapper are tested with synthetic events; capture calls are
   asserted via spies (jsdom's setPointerCapture absence is exactly why the
   adapter wraps it).
3. E2E (Playwright, chromium): real gesture specs — wheel pan/zoom both
   designers (mouse.wheel +/- ctrl), marquee (extend the existing rack-multi
   marquee drag spec; add a flat-canvas one), drag-port cabling (unblocked by
   stable `data-testid` on port rects), rejected-drop flash, Escape cancel.
   PLUS capture-regression specs that pin the historically buggy DOM-adapter
   layer (outside-voice finding 2, accepted): (a) start a drag, move the
   pointer outside the element/window, assert tracking continues and commits;
   (b) the v0.6.1 regression as a named spec — rack pan must not steal the
   subsequent click; (c) the Phase 8 regression class — capture target is the
   svg, not a parent. Layer 1 proves the machine; THESE prove the layer with
   the 2-for-2 shipped-bug record.
   Touch: Playwright touchscreen emulation (`hasTouch` context) for tap/drag;
   pinch via CDP `Input.synthesizePinchGesture`. If pinch synthesis proves
   flaky in CI, pinch stays covered at layer 1 with real recorded sequences
   and that exception is documented in the spec file header.

FIXTURE PROVENANCE (outside-voice finding 1, accepted): hand-authored
synthetic fixtures are the CI baseline and are labeled `synthetic: true`;
real-device traces (Mac trackpad two-finger + pinch, discrete-wheel mouse)
are recorded ONCE by the developer via the `?debug=input` logger — a named
HUMAN step inside M1, hardware inventory: the dev's own Mac + mouse. Touch
traces recorded when a touch device is available; until then touch fixtures
are synthetic-labeled. Synthetic fixtures prove machine logic; real traces
prove the normalizer against actual hardware. The distinction is never
blurred.

TEST ADDITIONS (eng review 2026-07-03, hardened by outside voice) — ORDERING
RULE: the three capture-regression e2e specs are the FIRST commit of M1,
before the skeleton lands — they only protect the surgery if they precede it.
- Keyboard router: unit tests for full routing ORDER (overlays beat
  gesture-cancel beat canvas shortcuts beat app shortcuts), StrictMode
  register/unregister idempotency, fault isolation WITH aborted continuation
  (cancel throws -> undo not applied, Cmd+S unaffected), and
  unregister-during-active-gesture (cancels + releases capture + reverts).
- Undo-consume: L1 sequence — Cmd+Z mid-drag reverts the drag and leaves
  history length unchanged; second Cmd+Z pops history.
- Escape-innermost: L1 sequence — Escape presses from overlay-open +
  active-gesture state land on close-overlay, cancel-gesture, clear-armed,
  clear-selection, in order, one per press.
- prefs parsing: wheelAction read from localStorage with a garbage value
  falls back to 'pan' (same union-guard pattern as getConnectMode).
- Wheel-mode behavior: e2e — flat canvas plain wheel PANS by default;
  flipping the Settings toggle to 'zoom' makes plain wheel zoom; toggle back
  restores pan.
- RAF coalescing: L2 — N pointermove events within one frame produce exactly
  one flushed state write whose value equals sequential application; pending
  flush is forced on cancel and dropped on unmount.
- RackRow adoption: e2e pan spec in row view (the third consumer must not be
  the untested one).
- M3 selection toolbar (contract restated: the toolbar IS interactive within
  its visible bounds and never beyond them): e2e for (a) pointerdown ON the
  toolbar operates the toolbar and starts no canvas gesture, (b) wheel over
  the toolbar does not pan/zoom the canvas, (c) drag-through while capture is
  held by the canvas is unaffected by the toolbar overlapping the path.
- M4b quick-create: e2e for the full flow — drag link, release on empty,
  pick type, assert node created + connected + selected + validated.
- M4d cross-rack drag: e2e including the reject path (drop on a full rack ->
  red flash + reason, device stays put).

SAFARI EXCEPTION (outside-voice finding 3, accepted): the Safari
`gesturestart`/`gesturechange` pinch branch is UNPROVABLE UNDER THIS HARNESS
(Playwright's WebKit is not Safari and does not emit those events). It ships
with unit coverage of the mapping function only, plus a manual QA checklist
item in the release notes. Named exception, not silent.

### Intentional behavior changes (enumerated — this plan CHANGES behavior)

All existing tests must pass, AND these deliberate changes ship with new tests:
1. Rack wheel: zoom-by-default -> pan-by-default (pref restores old feel).
2. Rack zoom floor 0.2 -> 0.1.
3. Escape: no longer clears selection while cancelling a marquee/gesture.
4. Cmd+Z (and App-level Cmd shortcuts) in text fields -> native text behavior,
   not model undo; `isTextTarget` gains SELECT.
5. Rejected rack drop/nudge: silent no-op -> red flash + reason.
6. Rack gesture thresholds: SVG-units -> screen px (feel change at zoom != 1).
7. Cmd+Z during an active drag: the keypress is consumed by the cancel (drag
   reverts, history untouched); a second press performs the undo. If the
   cancel throws, the undo is aborted — never applied over a live gesture.

## Milestones (each lands green: typecheck + the full unit suite + e2e)

| M | Content | Effort (human / CC) | Risk |
|---|---|---|---|
| M1 | Capture-regression e2e FIRST commit, then `src/input/` core + normalizeWheel + prefs + FLAT canvas adoption via strangler-fig with the legacy shim (skeleton + wheel/pan/pinch first, then per-gesture migration, every commit green — see Adapter & router contract); keyboard router (flat + app + overlays; rack listener stays until M2); layer-1/2 tests + fuzz + human fixture-recording step | ~1.5 wk / ~4 h | Med-High — over half the plan's risk lives here; the coexistence protocol (shim + capture ownership between old/new paths) is the actual cost center, not the feature count |
| M2 | RackCanvas + RackRow adoption. In-tree diff disposition (DECIDED, outside-voice finding 7): the uncommitted RackCanvas work stays uncommitted until M2 lands it corrected — KEEP its CSS-transform viewport plumbing, fit logic, and zoom widget; DISCARD its wheel-always-zoom handler (violates the contract; avoids a user-visible flip-flop). Rejected-drop feedback; fit-on-rack-switch; e2e gesture specs; RackDesigner keydown consolidates onto the router HERE (rack machines now exist). (A cablePath-extraction bundle was removed — already shipped in 2ad076d as src/rack/cablePath.ts) | ~4 d / ~1.5 h | Med |
| M3 | Quiet canvas + in-canvas cable ops (spec below). The floating selection toolbar is a FEATURE, not polish: it must never intercept canvas gestures (hit-test exclusion, same mechanism as wheel scoping) | ~4-5 d / ~1.5 h | Med |
| M4a | Expansion 1: touch/tablet pinch + two-finger pan | ~3-4 d / ~1.5 h | Med |
| M4b | Expansion 2: drop-on-empty quick-create (flat) | ~2 d / ~45 m | Low |
| M4c | Expansion 3: double-click quick-add (both) | ~1 d / ~30 m | Low |
| M4d | Expansion 4: cross-rack pointer drag (row view) | ~2-3 d / ~1 h | Med |
| M4e | Expansion 5: click-click cabling (rack). (An endpoint-validation bundle was removed — `connectRackCable` already validates via `hasEnd`, projectStore.ts:1942, shipped 2ad076d with the test) | ~1 d / ~30 m | Low |
| M4f | Expansion 6: MiniMap/RackRow perf hardening | ~2 d / ~1 h | Low |

M4a-f are independently landable in any order (each lands green on its own);
a stall on one does not block the others.

## Quiet canvas — concrete spec (M3)

- Selection toolbar (floating, near selection, both designers): flat = align/
  distribute (absorbs AlignBar), bring/send, group, delete, color; rack device
  = nudge U up/down, unmount, delete, move-to-rack; rack cable = color swatch,
  label, length, delete (this IS the in-canvas cable ops item).
- Idle demotion: mode toolbar and zoom bar drop to 60% opacity (WCAG-safe
  floor; 40% was rejected — outside-voice finding 10b) after 4 s without
  pointer activity over them; full opacity on hover/use AND on keyboard
  focus; the active tool indicator is never demoted; honors
  prefers-reduced-motion (no fade animation, instant states). No layout
  shift, opacity only (no motion in peripheral vision — Figma UI3 lesson).
- Collapsible: Inspector and BottomPanel get collapse toggles with state
  persisted in prefs; default state unchanged (no surprise removal).
- Nothing is deleted from the UI in M3 except the dead library filter pills;
  everything else is demoted or collapsed, reversibly.
- In-canvas cable ops rationale: the brief's "everything can be done inside
  the canvas too" is broader than creation-only; cable edit/delete currently
  requires the schedule panel, which is precisely the panel-round-trip the
  brief objects to. Reviewer flagged this as expansion-shaped; user brief
  language keeps it baseline.

## Vision

### 10x Check
NexMap becomes the tool where any pointing device just works, in either
designer, with identical muscle memory. The input core is platform
infrastructure: the same pure state machine later drives PEN input and
ISOMETRIC-mode gestures with no rewrite (touch ships now, in scope). FossFLOW's
one remaining edge ("frictionless feel") closes while its upstream repo is
abandoned; NexMap keeps its moat (validate-as-you-draw, 100% local, CSP +
netguard enforced).

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | Touchscreen/tablet gestures (two-finger pan + pinch, pointerType-aware) | M | ACCEPTED | Natural extension of shared core; README flags mobile ergonomics as untested |
| 2 | Drop connection on empty canvas -> quick-create device (flat) | M | ACCEPTED | FigJam-pattern flow accelerator; pure addition |
| 3 | Double-click quick-add (flat: picker at point; rack: place armed preset at U) | S | ACCEPTED | Cheap, discoverable, in-canvas-first |
| 4 | Cross-rack pointer drag in row view (replaces HTML5 DnD; live U preview + reject reason) | M | ACCEPTED | Completes on-canvas story; HTML5 DnD is touch-incompatible |
| 5 | Click-click cabling in rack designer (connect-mode pref parity with flat) | S | ACCEPTED | Tiny jack targets favor click-click on trackpads; FossFLOW-validated default |
| 6 | Perf hardening at scale (MiniMap culling/throttle, RackRow memoization, perf test) | M | ACCEPTED | Load-dimension edge case; hot paths MiniMap.tsx:90, RackRow.tsx:267 |

Why 6/6 accepted (reviewer challenged this): each was presented individually
with neutral effort/risk and the user opted in explicitly. The brief's own
words invite adjacent experience upgrades ("...and other experiences like that
when using both designers"), which covers #2/#3/#4/#5 directly; #1 (touch)
exceeds the literal "mouse and touchpad" brief and was accepted as an informed
stretch; #6 is the load-axis of "all edge cases handled." Sequencing protects
the baseline: all six live in M4, behind M1-M3.

## Accepted Scope (added to this plan)

Baseline (user brief, non-negotiable): unified wheel contract + preference;
gesture lifecycle safety (pointercancel, Escape-cancel, e.buttons, second-
pointer policy); screen-px thresholds; zero silent failures (reject reasons);
input-focus sanity (text-field guard + SELECT, undo-during-gesture abort);
bug kills (stale projection closure, rack fit-on-switch, justMarqueed leak,
dead filter pills); quiet canvas per M3 spec; in-canvas cable ops; proof
harness per the three-layer strategy.

Expansions: all six above (M4).

## NOT in scope
- Library-palette drag stays HTML5 DnD for now (explicit decision, not an
  omission): expansion 4 replaces DnD only on the cross-rack move path. Touch
  users place gear via the existing click-to-arm + tap-bay flow and the new
  double-click quick-add (M4c). Pointer-native palette drag is a named
  follow-up candidate.
- Isometric-specific new gestures (Phase 9 is shipped; core must not regress it)
- Pen/stylus-specific behaviors (future payoff of the pure core)
- Rebindable hotkeys, radial menus (lower-priority candidates, not scheduled)
- Connector-label drag-reposition (existing deferral, unchanged)
- Any cloud/server anything (local-only enforced by CSP + netguard)

## Deferred to TODOS.md
- (none from this ceremony — all six candidates accepted)

## Constraints
- No new runtime dependencies for the input core (from-scratch requirement; no
  tldraw/interactjs/use-gesture imports)
- Local-only: no network paths added; CSP default-src 'self' + netguard stay
- Undo/redo: every gesture commits exactly one undoable entry (existing
  History.mergeWith discipline)
- Existing tests pass; intentional behavior changes are enumerated above and
  each ships with its own new test
