import { defineConfig, devices } from '@playwright/test';

/**
 * End-to-end smoke tests for the critical canvas flows that unit tests can't
 * cover (real DOM, gestures, service worker, persistence). Runs against the Vite
 * dev server; in CI a fresh Chromium is installed and the server is started here.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: process.env.CI ? 'github' : 'list',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
